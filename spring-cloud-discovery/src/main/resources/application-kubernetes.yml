eureka:
  instance:
    hostname: eureka-0 # "${HOSTNAME}.eureka"
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url: 
      defaultZone: http://eureka-0.eureka:8761/eureka # ${EUREKA_SERVER_ADDRESS}